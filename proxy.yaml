proxy:
  host_proxy: 0.0.0.0
  port_proxy: 5000
  # maps of microservices with routes 
  enpoints_proxy:
      - name: microA
        services:
        - host_uri: http://localhost:3000/api/v1/health/
          path_to_map: /health/
        - host_uri: http://localhost:3000/api/v1/version/
          path_to_map: /version/

